# Frontend env. Copy to .env.local and fill in to use the real backend.
# Leave VITE_USE_BUTTERBASE unset (or false) to run against the in-memory mock.

VITE_USE_BUTTERBASE=false
VITE_BUTTERBASE_APP_ID=
VITE_BUTTERBASE_API_URL=https://api.butterbase.ai
VITE_BUTTERBASE_ANON_KEY=
