# Frontend env. Copy to .env.local and fill in — there is no mock/demo mode,
# the app always talks to the real deployed Butterbase backend.

VITE_BUTTERBASE_APP_ID=
VITE_BUTTERBASE_API_URL=https://api.butterbase.ai
VITE_BUTTERBASE_ANON_KEY=
